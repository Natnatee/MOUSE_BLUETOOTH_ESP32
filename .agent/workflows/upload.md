---
description: Build and upload code to ESP32 using PlatformIO
---
1. เช็คความพร้อมของสาย USB และพอร์ทคอมพิวเตอร์
⚠️ บังคับ (หากอัปโหลดไม่เข้า): กดปุ่ม Boot (IO0) ค้างไว้ขณะสั่งรัน หรือเช็คสถานะ Pin
2. รันคำสั่งต่อไปนี้ (ก๊อปปี้ไปวางใน Terminal):

```bash
pio run -t upload
```
